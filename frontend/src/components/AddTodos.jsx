import React from 'react'
import { useForm } from 'react-hook-form'

const AddTodos = () => {

    const {
        register,
        handlesubmit,
        watch,
        formState: { errors },
    } = useForm();

  return (
    <div>
        
    </div>
  )
}

export default AddTodos